---
// A stand-in for the MDX body `render()` would normally produce. The real
// content collection has no store to read from inside the test runner, so
// section-widgets.test.ts substitutes this for the prose and asserts only
// on what mounts around it.
---

<div data-mock-content>mock section body</div>
